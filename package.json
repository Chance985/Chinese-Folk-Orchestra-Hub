{
  "name": "chinese-folk-orchestra-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Chinese Folk Orchestra Hub full-stack final project",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix mui\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix mui",
    "seed": "npm run seed --prefix server",
    "build": "npm run build --prefix mui",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
